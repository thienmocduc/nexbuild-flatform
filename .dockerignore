# Git + CI metadata
.git
.gitignore
.github

# Env (secrets injected via ZeniCloud Secret Manager)
.env
.env.local
.env.production
.env.*.local

# Python build artifacts
__pycache__
*.pyc
*.pyo
*.pyd
.pytest_cache
.mypy_cache
.ruff_cache
*.egg-info

# Local SQLite (production uses ZeniCloud Cloud SQL)
*.db
*.sqlite
*.sqlite3
nexbuild_dev.db

# Frontend artefacts (Dockerfile.frontend ignores backend/, backend ignores public/)
# These are kept by default for backend build; .dockerignore.frontend can override

# Docker / Compose (not needed inside images)
docker-compose.yml
nginx

# Vercel / Railway leftovers
.vercel
.vercelignore
.railway

# Old serverless wrappers (now in ZeniCloud /email/send)
api/send-verification.js
api/verify-email.js
api/package.json

# IDE / OS
.claude
.vscode
.idea
.DS_Store
Thumbs.db

# Docs / scripts (not runtime)
docs
*.md
scripts

# Local uploads (production uses ZeniCloud GCP Storage)
uploads

# Plans / handoffs
ZENI_*.md
ZENIOS_*.md
IOS_*.md

# Test artifacts
test_*.py
design_result.json
